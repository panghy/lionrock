gradle.settingsEvaluated { settings ->
    settings.buildCache {
        remote(HttpBuildCache) {
            url = System.getenv('BUILD_CACHE_ADDR')
            push = true
            credentials {
                username = System.getenv('BUILD_CACHE_USERNAME')
                password = System.getenv('BUILD_CACHE_PASSWORD')
            }
        }
    }
}